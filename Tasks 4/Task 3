"""
Реализовать функцию int_func(), принимающую слово из маленьких латинских букв и возвращающую его же, но
с прописной первой буквой. Например, print(int_func(‘text’)) -> Text.
Продолжить работу над заданием. В программу должна попадать строка из слов, разделенных пробелом. Каждое
слово состоит из латинских букв в нижнем регистре. Сделать вывод исходной строки, но каждое слово должно
начинаться с заглавной буквы. Необходимо использовать написанную ранее функцию int_func().
"""

str = input('Введите слово из маленьких латинских букв')

a_new_var1 = []
a_new_var2 = []


def int_func(word):
    # Перевое задание
    word_caps = word.swapcase()
    print('Первое задание:', word_caps)

    # с использованием старой функции (запись длиннее)
    a = word.split(' ')
    for i in a:
        First_chart = i[0].swapcase()
        Word_new = First_chart + i[1:]
        a_new_var1.append(Word_new)
    str_new_var1 = ' '.join(a_new_var1)
    print('С использованием старой функции', str_new_var1)

    # без использования старой функции (запись короче)
    a = word.split(' ')
    for i in a:
        t = i.capitalize()
        a_new_var2.append(t)
    str_new_var2 = ' '.join(a_new_var2)
    print('Без использования старой функции', str_new_var2)


int_func(str)
