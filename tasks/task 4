# 4. Пользователь вводит целое положительное число. Найдите самую большую цифру в числе.
# Для решения используйте цикл while и арифметические операции.

w = int(input("Введите целое положительное число"))
wStr = str(w)
max = 0
i = 0
while i < int(len(wStr)):
    print(wStr[i])
    if max < int(wStr[i]):
        max = int(wStr[i])
    i += 1
print("Самая большая цифра в числе равна {}.".format(max))
