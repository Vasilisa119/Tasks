"""
1. Реализовать функцию, принимающую два числа (позиционные аргументы) и выполняющую их деление. Числа запрашивать
у пользователя, предусмотреть обработку ситуации деления на ноль.
"""

# Ввод чисел
a = int(input("Введите делимое (целое число)"))
b = int(input("Введите делитель (целое число)"))


def division(x, y):
    """
    Функция деления
    :param x: делимое
    :param y: делитель
    :return:
    """
    try:
        print(x / y)
    except ZeroDivisionError:
        print("На ноль делить нельзя!")


division(a, b)
