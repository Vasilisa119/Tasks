"""
 3. Пользователь вводит месяц в виде целого числа от 1 до 12. Сообщить к какому времени года
 относится месяц (зима, весна, лето, осень). Напишите решения через list и через dict.
"""

# Ввод числа пользователем
try:
    number_user = int(input('Введите номер месяца (целое число от 1 до 12)'))
except ValueError:
    print('Вы ввели не корректное значение')
    exit(0)

# Решение через list
list_month = ['зима',
              'весна',
              'лето',
              'осень',
              'зима',
              ]
month_from_list = number_user // 3
if 0 < number_user < 13:
    print('Решение через списки: ', list_month[month_from_list])
else:
    print('Вы ввели число не в диапазоне 1..12')
    exit(0)

# Решение через dict
dict_month = {
    1: 'зима',
    2: 'зима',
    3: 'весна',
    4: 'весна',
    5: 'весна',
    6: 'лето',
    7: 'лето',
    8: 'лето',
    9: 'осень',
    10: 'осень',
    11: 'осень',
    12: 'зима',
}
try:
    print('Решение через словари: ', dict_month[number_user])
except KeyError:
    print('Вы ввели не корректное значение')
    exit(0)
    
